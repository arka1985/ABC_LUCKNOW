"    // Asynchronously load the uploaded Lucknow ward boundary GeoJSON data\n    fetch('LUCKNOW/lucknow_ward_boundary.geojson')\n      .then(response => response.json())\n      .then(data => {\n        console.log(\"[GIS Map] Loaded Lucknow ward boundary GeoJSON successfully.\");\n        window.LUCKNOW_GEOJSON = data;\n        window.renderLucknowChoropleth();\n        window.buildCascadingFilters();\n        window.updateMapMarkers();\n        window.renderAlertClusters();\n        window.renderTemporalTrendLine();\n      })\n      .catch(err => {\n        console.error(\"[GIS Map] Error loading LUCKNOW geojson file (using standard fallback for offline/local view):\", err);\n        // Fallback to render active markers, alerts, and trend line even if ward boundaries are offline/blocked by CORS\n        window.updateMapMarkers();\n        window.renderAlertClusters();\n        window.renderTemporalTrendLine();\n      });"