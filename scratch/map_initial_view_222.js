I will write a helper script `scratch/find_history.js` to search the conversation transcript log `transcript.jsonl` for the original Leaflet map code or any previous `map.js` implementation, so we can see how the Leaflet background map and markers were set up originally. This will guide us in cleanly integrating Leaflet as the background layer under the Lucknow Municipality highlights.